# Amazon — Frontend (React + Vite)

This folder contains the frontend for the Amazon e-commerce UI (educational project).

Getting started

1. cd client
2. npm install
3. npm run dev (runs on port 5173)

Notes

- The frontend uses local seed data in `src/data/products.js` for development.
- API integration and auth are not yet implemented.
